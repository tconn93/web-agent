# Project Plan

Build a Web UI to work with AI agents on development projects that are on the server that this app is deployed to. 

We will also need an Orchestrator API that will handle deploying and communicating with the agents through the web UI. 